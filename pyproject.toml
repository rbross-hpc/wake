[build-system]
requires      = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name            = "wake"
version         = "0.4.27"
description     = "Evidence-backed impact analysis for research papers"
readme          = "README.md"
requires-python = ">=3.10"
license         = "BSD-3-Clause"
authors         = [{ name = "Robert Ross", email = "rross@anl.gov" }]
dependencies = [
    "requests>=2.31",
    "openai>=1.0",
    "tenacity>=8.0",
    "pyyaml>=6.0",
    "python-dotenv>=1.0",
    "pydantic>=2.0",
]

[project.optional-dependencies]
# httpx is a real test dependency (tests/test_openai_client.py constructs
# real openai.APIStatusError instances via httpx.Response/httpx.Request),
# not just something that happened to come along with openai -- declare
# it explicitly rather than relying on it transitively. It used to ride
# in for free via openai<3's own dependency on httpx; openai 3.x switched
# to a differently-importable package instead, which silently broke CI
# (see docs/build-log.md's v0.4.26 entry).
dev = ["pytest", "pytest-mock", "pytest-cov", "ruff", "mypy", "httpx"]
pdf = ["pypdf>=4.0", "pdfplumber>=0.10"]

[project.scripts]
wake = "wake.cli.main:main"

[tool.setuptools.packages.find]
where   = ["."]
include = ["wake*"]

[tool.setuptools.package-data]
wake = ["config.yaml", "skills/impact-analysis/**/*"]

[tool.pytest.ini_options]
markers = [
    "network: marks tests that make real network calls (deselect with -m 'not network')",
    "slow: marks golden-packet tests that copy+rebuild a full packet (run with -m slow or as part of the full suite)",
]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = [
    "E501",  # line length handled loosely; long strings/URLs are common in prompts and docs
]

[tool.mypy]
python_version = "3.10"
files = ["wake"]
ignore_missing_imports = true
warn_unused_ignores = true
warn_redundant_casts = true
check_untyped_defs = true
# Baseline is intentionally lenient: wake's domain model is still dict-based
# (see PLAN.md "Phase 3 - Structural Hardening" / refactor/domain-models).
# Tighten (disallow_untyped_defs, strict_optional, etc.) as typed models land.
disallow_untyped_defs = false
no_implicit_optional = true
# Scoped to the wake/ package only -- tests are exercised for correctness
# by pytest, not for typing; adding mypy across tests/ is a separate,
# much larger cleanup with little safety payoff.
